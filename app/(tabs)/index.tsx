import { router } from "expo-router";
import { FlatList, Pressable, StyleSheet, Text, View } from "react-native";
import { IconSymbol } from "@/components/ui/icon-symbol";
import { Card, EmptyCard, LoadingView, ModulePill, PrimaryButton, QuietButton, SectionLabel, StatusPill, palette } from "@/components/workspace-ui";
import { ScreenContainer } from "@/components/screen-container";
import { formatDate, moduleById, STACK_MODULES } from "@/lib/workspace-model";
import { summarizeDispatchHistory } from "@/lib/dashboard";
import { useWorkspace } from "@/lib/workspace-context";
import { trpc } from "@/lib/trpc";

type HealthTone = "healthy" | "unavailable" | "manual" | "unknown";
const healthColor: Record<HealthTone, string> = { healthy: "#74D6A1", unavailable: palette.rose, manual: palette.amber, unknown: "#708093" };

export default function DashboardScreen() {
  const { ready, jobs, workspaces } = useWorkspace();
  const dashboard = summarizeDispatchHistory(jobs);
  const statusQuery = trpc.gateway.status.useQuery(undefined, { retry: false });
  const healthQuery = trpc.gateway.health.useQuery(undefined, { enabled: statusQuery.data?.configured === true, retry: false, refetchInterval: 15_000, refetchOnWindowFocus: true });
  if (!ready) return <LoadingView />;
  const healthMap = new Map(healthQuery.data?.modules.map((module) => [module.id, module.status]) ?? []);
  const configured = statusQuery.data?.configured === true;
  const lastChecked = healthQuery.dataUpdatedAt ? new Date(healthQuery.dataUpdatedAt).toLocaleTimeString([], { hour: "numeric", minute: "2-digit" }) : null;
  return <ScreenContainer containerClassName="bg-[#101620]" safeAreaClassName="bg-[#101620]"><FlatList data={dashboard.recentDispatches} keyExtractor={(item) => item.id} contentContainerStyle={styles.content}
    ListHeaderComponent={<><View style={styles.header}><View><Text style={styles.eyebrow}>OPERATIONS OVERVIEW</Text><Text style={styles.title}>Dashboard</Text></View><Pressable accessibilityRole="button" accessibilityLabel="Open gateway controls" onPress={() => router.push("/gateway")} style={({ pressed }) => [styles.gatewayButton, pressed && styles.pressed]}><IconSymbol name="lock.shield" size={19} color={palette.teal} /></Pressable></View><Card style={styles.healthCard}><View style={styles.healthTop}><View style={styles.healthTitleWrap}><View style={styles.healthGlyph}><IconSymbol name="server.rack" size={18} color={configured ? palette.teal : palette.amber} /></View><View><Text style={styles.healthTitle}>Live stack health</Text><Text style={styles.healthDetail}>{healthQuery.isFetching ? "Refreshing operator-authorized status…" : configured ? healthQuery.data?.status ?? "Awaiting health response" : "Gateway has not been configured"}</Text></View></View><Pressable accessibilityRole="button" accessibilityLabel="Refresh stack health" onPress={() => void healthQuery.refetch()} style={({ pressed }) => [styles.refreshButton, pressed && styles.pressed]}><IconSymbol name="arrow.clockwise" size={17} color={palette.teal} /></Pressable></View><View style={styles.moduleGrid}>{STACK_MODULES.map((module) => { const state = (healthMap.get(module.id) ?? "unknown") as HealthTone; return <View key={module.id} style={styles.healthModule}><View style={[styles.healthDot, { backgroundColor: healthColor[state] }]} /><Text style={styles.healthModuleName}>{module.shortName}</Text><Text style={[styles.healthModuleState, { color: healthColor[state] }]}>{state === "unknown" ? "Awaiting" : state === "manual" ? "Manual" : state === "healthy" ? "Online" : "Unavailable"}</Text></View>; })}</View><View style={styles.healthFoot}><Text style={styles.healthFootText}>{statusQuery.error ? "Operator sign-in is required for health visibility." : lastChecked ? `Auto-refreshes every 15 seconds · checked ${lastChecked}` : "Configure the private gateway to activate automatic health refresh."}</Text><QuietButton label="Gateway" onPress={() => router.push("/gateway")} icon="chevron.right" /></View></Card><View style={styles.metrics}><Metric label="Local dispatches" value={String(dashboard.totalDispatches)} /><Metric label="In review" value={String(dashboard.reviewCount)} /><Metric label="Accepted" value={String(dashboard.acceptedCount)} /></View><View style={styles.sectionGap}><SectionLabel action={<QuietButton label="Prepare analysis" onPress={() => router.push("/analysis/new")} icon="plus" />}>Dispatch history</SectionLabel></View></>}
    renderItem={({ item }) => <Pressable accessibilityRole="button" accessibilityLabel={`Open dispatched analysis ${item.reference}`} onPress={() => router.push({ pathname: "/analysis/[id]", params: { id: item.id } })} style={({ pressed }) => [styles.dispatchRow, pressed && styles.pressed]}><View style={styles.dispatchBody}><View style={styles.dispatchTop}><Text style={styles.dispatchRef} numberOfLines={1}>{item.reference}</Text><StatusPill state={item.state} /></View><View style={styles.moduleRow}>{item.modules.map((id) => <ModulePill key={id} id={id} />)}</View><Text style={styles.dispatchMeta}>{item.gatewayStatus ?? "Accepted"} · {item.dispatchedAt ? `Dispatched ${formatDate(item.dispatchedAt)}` : "Local record"}</Text></View><IconSymbol name="chevron.right" size={18} color={palette.muted} /></Pressable>}
    ItemSeparatorComponent={() => <View style={{ height: 10 }} />}
    ListEmptyComponent={<EmptyCard title="No local dispatches yet" detail="Approved jobs will appear here after the operator gateway accepts them. Module coverage remains local and never fabricates a remote result." action={<PrimaryButton label="Open gateway controls" onPress={() => router.push("/gateway")} icon="lock.shield" />} />}
    ListFooterComponent={<View style={styles.coverage}><SectionLabel>Module coverage</SectionLabel><View style={styles.coverageGrid}>{STACK_MODULES.map((module) => <View key={module.id} style={styles.coverageItem}><View style={[styles.coverageMark, { backgroundColor: module.tint }]} /><Text style={styles.coverageName}>{module.shortName}</Text><Text style={styles.coverageValue}>{dashboard.moduleCounts[module.id]}</Text></View>)}</View></View>}
  /></ScreenContainer>;
}
function Metric({ label, value }: { label: string; value: string }) { return <View style={styles.metric}><Text style={styles.metricValue}>{value}</Text><Text style={styles.metricLabel}>{label}</Text></View>; }
const styles = StyleSheet.create({ content: { paddingHorizontal: 20, paddingTop: 10, paddingBottom: 34, gap: 12, backgroundColor: palette.base, flexGrow: 1 }, header: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", marginBottom: 4 }, eyebrow: { color: palette.teal, fontSize: 11, fontWeight: "800", letterSpacing: 1.2 }, title: { color: palette.text, fontSize: 31, lineHeight: 38, fontWeight: "800", letterSpacing: -0.6, marginTop: 3 }, gatewayButton: { width: 42, height: 42, alignItems: "center", justifyContent: "center", borderWidth: 1, borderColor: "#276572", borderRadius: 14, backgroundColor: "#142833" }, healthCard: { backgroundColor: "#142833", borderColor: "#214A53" }, healthTop: { flexDirection: "row", alignItems: "center", justifyContent: "space-between", gap: 10 }, healthTitleWrap: { flexDirection: "row", alignItems: "center", gap: 10, flex: 1 }, healthGlyph: { width: 36, height: 36, borderRadius: 12, backgroundColor: palette.tealMuted, alignItems: "center", justifyContent: "center" }, healthTitle: { color: palette.text, fontSize: 15, fontWeight: "800" }, healthDetail: { color: palette.muted, fontSize: 12, marginTop: 3 }, refreshButton: { width: 38, height: 38, borderRadius: 12, alignItems: "center", justifyContent: "center", borderColor: "#2A5962", borderWidth: 1 }, moduleGrid: { flexDirection: "row", flexWrap: "wrap", gap: 8, marginTop: 16 }, healthModule: { width: "31%", minHeight: 61, borderRadius: 13, backgroundColor: "#101C27", padding: 10, gap: 3 }, healthDot: { width: 7, height: 7, borderRadius: 4 }, healthModuleName: { color: palette.text, fontSize: 12, fontWeight: "800" }, healthModuleState: { fontSize: 10, fontWeight: "800" }, healthFoot: { flexDirection: "row", alignItems: "center", justifyContent: "space-between", gap: 8, marginTop: 14, borderTopColor: "#214A53", borderTopWidth: 1, paddingTop: 11 }, healthFootText: { color: palette.muted, fontSize: 11, lineHeight: 16, flex: 1 }, metrics: { flexDirection: "row", gap: 9 }, metric: { flex: 1, padding: 14, minHeight: 85, borderColor: palette.border, borderWidth: 1, backgroundColor: palette.surface, borderRadius: 17, justifyContent: "space-between" }, metricValue: { color: palette.text, fontSize: 23, lineHeight: 28, fontWeight: "800" }, metricLabel: { color: palette.muted, fontSize: 11, lineHeight: 14, fontWeight: "600" }, sectionGap: { marginTop: 10 }, dispatchRow: { flexDirection: "row", alignItems: "center", gap: 12, padding: 16, backgroundColor: palette.surface, borderColor: palette.border, borderWidth: 1, borderRadius: 18 }, dispatchBody: { flex: 1, gap: 8 }, dispatchTop: { flexDirection: "row", alignItems: "center", justifyContent: "space-between", gap: 8 }, dispatchRef: { color: palette.text, fontSize: 15, fontWeight: "800", flex: 1 }, moduleRow: { flexDirection: "row", flexWrap: "wrap", gap: 6 }, dispatchMeta: { color: palette.muted, fontSize: 12 }, coverage: { marginTop: 20, marginBottom: 10 }, coverageGrid: { flexDirection: "row", flexWrap: "wrap", gap: 8 }, coverageItem: { width: "31%", flexDirection: "row", alignItems: "center", gap: 7, backgroundColor: palette.surface, borderWidth: 1, borderColor: palette.border, borderRadius: 12, paddingHorizontal: 9, paddingVertical: 10 }, coverageMark: { width: 7, height: 7, borderRadius: 4 }, coverageName: { color: palette.muted, fontSize: 11, fontWeight: "700", flex: 1 }, coverageValue: { color: palette.text, fontSize: 14, fontWeight: "800" }, pressed: { opacity: 0.72 } });
